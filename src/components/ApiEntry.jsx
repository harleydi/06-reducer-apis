import React, { useState } from 'react'

const apiEntry = ({ results }) => {
  {results && console.log(results)}
  console.log(results)
  
  return (
    <div>
      
    </div>
  )
}

export default apiEntry